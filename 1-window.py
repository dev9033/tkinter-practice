import tkinter

window = tkinter.Tk()
# to rename the title of the window
window.title('gui')
# to set minimum window size
window.minsize(100,200)
# to size a window use
# window.geometry(100,200)
window.mainloop()
